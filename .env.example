# Optional overrides for radar settings
RADAR_DATA_DIR=data
RADAR_ARTIFACTS_DIR=artifacts
RADAR_LOG_LEVEL=INFO

# Alpha Vantage (chart comparison line). Do not commit the real key.
ALPHAVANTAGE_API_KEY=
